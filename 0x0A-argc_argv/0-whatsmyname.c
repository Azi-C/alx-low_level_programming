#include <stdio.h>
#include <stdlib.h>

/**
 * main-  program that prints its name
 * Return: the program's name
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	exit (EXIT_SUCCESS);
}
